import { assert, expect } from "chai";

describe("This is a demo test", () => {
      it("should return 5 when 2 is added to 3", () => {
      const result = 2 +  3;
      expect(result).eql(5);
   });
});